/**
 * Load environment variables from .env file.
 * Import this file at the top of any script to enable .env support.
 *
 * Usage:
 *   import "./load-env";
 *   import { getNetworkConfig } from "./network-config";
 */
import "dotenv/config";
